# Slice Proposals

This directory contains the live proposal backlog.

There are currently no active proposal waves checked into the repository.

The `0.6.0` functional waves and the follow-up combat audit closures have already been implemented and moved into `docs/slices/implemented/`.
