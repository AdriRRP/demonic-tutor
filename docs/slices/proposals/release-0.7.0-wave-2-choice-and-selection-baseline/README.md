# Release 0.7.0 - Wave 2 - Choice And Selection Baseline

Goal:

- unlock common card patterns that require explicit player choice beyond a single target

Slice count:

- `0`

Slices:

Why this wave has high return:

- it expands the usable card pool sharply while also forcing the prompt model the UI will need anyway
